# spdlog

The `spdlog` C++ logging library modified to build as part of the EPICS build system. The version used here is `v1.17.0`. The original source code can be found at [the spdlog GitHub repository](https://github.com/gabime/spdlog).
